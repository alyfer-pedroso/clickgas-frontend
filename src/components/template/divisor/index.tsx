import { FC } from "react";
import { View } from "react-native";

import { colors } from "@data/constants";

export const Divisor: FC = () => {
  return <View style={{ width: "100%", height: 1, backgroundColor: colors["third-gray"] }}></View>;
};
